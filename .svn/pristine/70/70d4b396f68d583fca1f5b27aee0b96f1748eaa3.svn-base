#include "syscall.h"

int main()
{
    int n;
    int tmp = GetInt(&n);
    // printf ("%dfuera\n", n);
    if (tmp != 0) {
        PutInt(n);
    }
    else {
        PutString("Pas un int\n");
    }
    
    return 0;
}